// ─────────────────────────────────────────────────────────────────────────────
// Tests unitaires pour 32_strict_operators.oc
// ─────────────────────────────────────────────────────────────────────────────

import ocara.UnitTest

class Test_32_strict_operators {

    public method equalityTest(): int {
        var x:int = 42
        var z:int = 42
        var y:int = 7

        UnitTest::assertTrue(x equal z)
        UnitTest::assertTrue(x not equal y)
        return 0
    }

    public method crossNumericEqualityTest(): int {
        // int et float sont l'unique paire de types compatible pour une
        // comparaison typée : widening numérique explicite, pas un bitcast.
        var a:int = 10
        var b:float = 10.0
        var c:float = 2.5

        UnitTest::assertTrue(a equal b)
        UnitTest::assertTrue(a not equal c)
        return 0
    }

    public method stringContentEqualityTest(): int {
        var name:string = "Alice"
        var otherName:string = "Alice"

        UnitTest::assertTrue(name equal otherName)
        return 0
    }

    public method orderingTest(): int {
        var num1:int = 100
        var num2:int = 200
        var num3:float = 100.0

        UnitTest::assertTrue(num1 smaller num2)
        UnitTest::assertTrue(num2 greater num1)
        UnitTest::assertTrue(num1 smaller or equal num3)
        UnitTest::assertTrue(num2 greater or equal num1)
        return 0
    }

    public method mainTest(): int {
        var x:int = 42
        var z:int = 42
        UnitTest::assertTrue(x equal z)

        var a:int = 10
        var b:float = 10.0
        UnitTest::assertTrue(a equal b)

        var num1:int = 100
        var num2:int = 200
        UnitTest::assertTrue(num1 smaller num2)
        UnitTest::assertTrue(num2 greater or equal num1)

        return 0
    }
}
