import ocara.IO

init {
    IO::writeln("Init")
}

main {
    IO::writeln("Main start")
    try {
        IO::writeln("In try block")
        raise 123
    } on e {
        IO::writeln(`Caught error: ${e}`)
        result e
    }
    IO::writeln("Main end")
}
