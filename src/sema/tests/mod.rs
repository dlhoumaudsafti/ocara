/// Tests unitaires du module sema — un fichier par domaine testé (voir
/// docs/roadmap.d/qualite-tests-unitaires-critiques.md).

mod escape;
mod resource_raise;
mod builtin_alias_resolution;
mod self_const_resolution;
mod resource_property_destructor;
